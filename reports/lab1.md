### 功能实现总结

- 使用数组在 TCB 中记录系统调用次数
- syscall 函数最开始次数递增
- 调用 trace 时查询记录的数据

### 遇到的几个问题

- trace 统计次数想用 Map，但是不支持 Copy，换成数组
- 一开始的数组太小，导致越界
- TCB 是 Copy 的，提取一个变量就 Copy 了一份，导致修改的是被 Copy 的，而不是原始的，导致修改失败
